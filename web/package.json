{
  "name": "orapa-web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build:wasm": "cd .. && cargo build --target wasm32-unknown-unknown --release -p orapa-wasm && wasm-bindgen target/wasm32-unknown-unknown/release/orapa_wasm.wasm --out-dir web/src/wasm --target web --typescript",
    "dev": "vite",
    "build": "npm run build:wasm && tsc -b && vite build",
    "lint": "oxlint",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "zustand": "^5.0.2"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.4",
    "oxlint": "^1.75.0",
    "typescript": "~6.0.2",
    "vite": "^8.2.0"
  }
}
